DROPBOX_APP_KEY=
DROPBOX_APP_SECRET=
DROPBOX_REFRESH_TOKEN=
DROPBOX_FOLDER=/retake/test

# Optional remote reprocessing providers, used from the web UI's "Reprocess
# selected" dropdown when the local model doesn't do a clean job. Each is
# independent -- set as many or as few as you want, leave blank to skip.
API4AI_API_KEY=
PIXELCUT_API_KEY=
